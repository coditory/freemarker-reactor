plugins {
    id("com.gradle.enterprise").version("3.5")
}

rootProject.name = "freemarker-reactor"

gradleEnterprise {
    buildScan {
        termsOfServiceUrl = "https://gradle.com/terms-of-service"
        termsOfServiceAgree = "yes"
    }
}
